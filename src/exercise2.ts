import { candidates } from './data';
import { Candidate } from './models';

function getCandidateEmails(candidates: Candidate[]): string[] {
	// Implementation goes here
	return [];
}

const emails = getCandidateEmails(candidates);
console.log(emails);
